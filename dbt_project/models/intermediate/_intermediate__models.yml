version: 2

models:
  - name: int_amazon__daily_ad_spend_by_campaign
    description: >
      Unified daily campaign-level ad spend across Sponsored Products,
      Sponsored Brands, and Sponsored Display.
    columns:
      - name: report_date
        tests: [not_null]
      - name: campaign_id
        tests: [not_null]
      - name: ad_product
        tests:
          - not_null
          - accepted_values:
              values: ['SPONSORED_PRODUCTS', 'SPONSORED_BRANDS', 'SPONSORED_DISPLAY']

  - name: int_amazon__daily_ad_spend_by_asin
    description: >
      Unified daily ASIN-level ad spend across Sponsored Products
      and Sponsored Display.
    columns:
      - name: report_date
        tests: [not_null]
      - name: campaign_id
        tests: [not_null]
      - name: asin
        tests: [not_null]
      - name: ad_product
        tests:
          - not_null
          - accepted_values:
              values: ['SPONSORED_PRODUCTS', 'SPONSORED_DISPLAY']

  - name: int_amazon__daily_orders
    description: "Daily order aggregations by ASIN from SP-API seller orders"
    columns:
      - name: purchase_date_utc
        tests: [not_null]
      - name: asin
        tests: [not_null]

  - name: int_amazon__daily_ad_spend
    description: "Daily ad spend aggregation by ASIN from SP advertised products"
    columns:
      - name: date
        tests: [not_null]
      - name: asin
        tests: [not_null]
